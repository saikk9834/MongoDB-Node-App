"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const RefParser = require("json-schema-ref-parser");
const deasync_1 = require("deasync");
exports.$RefParser = {
    bundle: (schema, options) => {
        var savedError, savedResult, done = false;
        RefParser.bundle(schema, options, (error, result) => {
            savedError = error;
            savedResult = result;
            done = true;
        });
        deasync_1.loopWhile(() => !done);
        if (savedError)
            throw savedError;
        return savedResult;
    },
};
//# sourceMappingURL=json.ref.schema.js.map