import { OpenAPIFrameworkArgs, OpenAPIFrameworkVisitor, OpenAPIV3 } from './types';
export declare class OpenAPIFramework {
    readonly apiDoc: OpenAPIV3.Document;
    readonly basePaths: string[];
    private readonly validateApiDoc;
    private readonly validator;
    private readonly basePathObs;
    private readonly loggingPrefix;
    constructor(args: OpenAPIFrameworkArgs);
    initialize(visitor: OpenAPIFrameworkVisitor): void;
    private loadSpec;
    private copy;
    private sortApiDocTags;
    private getBasePathsFromServers;
}
